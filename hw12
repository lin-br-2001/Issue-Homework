#include <stdio.h>
#include <math.h>

int main(void)
{
    double x;
    int n, c;
    
    printf("Enter x:");
    scanf("%f", &x);
    printf("Enter n:");
    scanf("%d", &n);
    
    for(c=0;c<=n;++1){
        r=pow(-1,n);
    }
    
}
